server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/craftiv_db
    username: craftiv_user
    password: 27Cliff2005
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
